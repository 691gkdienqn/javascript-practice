// 함수(Fuction)
// 입력 값을 받아서 데이터를 처리하고, 결과를 반환하는 코드 뭉치
// -> 코드를 재사용하기 위해 만들어놓은 구조

// 함수 정의 -> "~ 기능을 하는 코드 덩어리"를 X라는 이름으로 부르겠다.
// return -> 결과값을 반환 & 함수 종료 
function add(n1, n2) { 
    let result = n1 + n2; 
    // return result ; 
}

// 함수 호출(call) -> 함수를 사용한다.
console.log(add(1, 2));



// 위와 같이 실행하면 터미널에 나오는 값은? 
// >>  undefined  