// Python : None
// JS : null -> 값이 비어있음/없음 을 의미 

// Lotto 
let selectedUser = null;


console.log(selectedUser);
console.log(typeof selectedUser);  // JS 초기 구현이 object로 되어있음    (-> null 값이 object 로 출력됨)